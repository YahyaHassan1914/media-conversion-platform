services:
  redis:
    image: redis:8
    restart: unless-stopped

    ports:
      - "6379:6379"

    networks:
      - platform

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5